name: Schedule Test

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "Schedule funguje"
